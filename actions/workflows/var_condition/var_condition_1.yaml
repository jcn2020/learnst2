---
version: 1.0
description: print a string if input production_active is true

input:
  - production_active

vars:
  - prod_act

output: 
  - my_output: <% task(analyze).result.result %>



tasks: 
  analyze:
    action: learnst2.var_condition_1_checker
    input:
      production_active: <% ctx().production_active %>
    next:
      - when: <% succeeded() %>
        publish:
          - prod_act: <% task(analyze).result.stderr %>