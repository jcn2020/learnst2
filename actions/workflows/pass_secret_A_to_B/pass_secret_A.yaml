---
version: 1.0
description: pass secret from action A to action B of the same pack

input:
  - secret_A
  - execution_id: <% ctx(st2).action_execution_id %> 
  - secret_string: '{{st2kv.system.mytopsecret}}'

tasks:
  log_execution_id:
    action: core.local
    input:
      cmd: >
        echo "log execution id = <% ctx().execution_id %> " ; echo "secret string {{ st2kv.system.mytopsecret}}"
