---
name: cred_check
# pack
description: check cred access
enabled: true

runner_type: orquesta
entry_point: workflows/cred_check/cred_check.yaml


parameters:
  cred_1:
    type: string
    required: true
    secret: false
    default: "{{st2kv.system.cred_1 | decrypt_kv }}"
    description: "cred default for cred_1"

  