---
version: 1.0
description: print a string if input production_active is true

input:
- production_active


tasks: 
  analyze:
    action: learnst2.var_condition_1_checker
    input:
      production_active
    next:
      - when: <% succeeded() %>
        publish:
          - my_output: <% task(analyze).result.stdout %>