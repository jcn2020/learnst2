---
version: 1.0
description: "trigger concurrent jobs"

input: 
  - job_queue

vars: 
  - temp: nil

tasks:
  sleepy_job:
    action: core.local
    input: 
      cmd: "sleep 1"
  run_jobs:
    with: <% ctx(job_queue) %>
    action: core.local
    input: 
      cmd: "echo name = <% item() %>"





